---
apiVersion: humanitec.org/v1b1

resources:
  "dns":
    scope: shared

spec:
  "ingress":
    rules:
      "${resources.dns}":
        http:
          "/":
            type: prefix
            port: 80
